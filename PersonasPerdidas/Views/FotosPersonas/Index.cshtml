@model IEnumerable<PersonasPerdidas.Models.FotosPersona>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fotos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CrearPersonaPerdida.Nombre)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fotos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CrearPersonaPerdida.Nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id_Fotos }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id_Fotos }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id_Fotos })
            </td>
        </tr>
    }

</table>